# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: 470aa298-3a48-4750-8810-ace707ddce22

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
    default: "citibike_dev"
  schema:
    description: The schema to use
  dev_flag: 
    default: true

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405604461689833.13.azuredatabricks.net
    variables:
      catalog: citibike_dev
      schema: dev
    presets:
      artifacts_dynamic_version: true
  test:
    mode: production
    workspace:
      host: https://adb-7405608877276319.19.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/andrewkennett@akvisions.net to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: citibike_test
      schema: prod
      dev_flag: false
    permissions:
      - user_name: d96f07a7-08a7-4047-b43e-f12d04538f43
        level: CAN_MANAGE
    presets:
        name_prefix: '[test] '
    run_as:
      service_principal_name: d96f07a7-08a7-4047-b43e-f12d04538f43
  prod:
    mode: production
    workspace:
      host: https://adb-7405611485472087.7.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/andrewkennett@akvisions.net to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: citibike_prod
      schema: prod
      dev_flag: false      
    permissions:
      - user_name: 41c3b301-9ace-4785-b8a4-7fb04855e08a
        level: CAN_MANAGE
    run_as:
      service_principal_name: 41c3b301-9ace-4785-b8a4-7fb04855e08a

